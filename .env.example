# PIN that unlocks the document vault (client/public/vault). Choose something
# only Georgie's team knows — this is not a strong secret, just a soft gate.
VAULT_PORTAL_PIN=

# Random signing secret for vault session cookies. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VAULT_PIN_SECRET=
